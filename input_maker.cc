#include <iostream>
using namespace std;
int i,j,k,n,m;

int main()
{
    cin >> n >> m; // n=입력파일 개수, m=입력란 개수

    for (i = 0; i < n; i++)
    {
        // m줄에 걸쳐서 입력 숫자 범위 (min, max)

    }
    
    for (i = 0; i < n; i++)
    {
        sstream infile_name;
        infile_name << "Input_" << i;
        ifstream infile(infile_name);
        for (j = 0; j < m; j++)
        {
            infile << 
        }
    }
}