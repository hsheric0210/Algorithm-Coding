// 최적화:
// 1. 4개 사분면 모두가 아닌 단 1개 사분면의 점 개수만 알아도 나머지 3개 사분면의 점들 개수를 모두 알 수 있다.
// ㄴ 1개 사분면 위에 있는(축 제외) 점들 개수를 모두 구한 후, '* 4'를 해주면 된다.
//  ㄴ 각 사분면은 또 y=x(또는 y=-x)를 기준으로 절반으로 자를 수 있으며, 이 선을 기준으로 양쪽의 점 개수는 같다.
// 2. 반지름이 정수이고 각 원 위 점들을 포함해서 세기에, 기본적으로 각 축 위에 점들이 (r2-r1+1)개는 존재한다.
// ㄴ 4개 방향 축(X+, X-, Y+, Y-)을 모두 합하면, 적어도 4*(r2-r1+1)개의 점들은 반드시 존재한다.
// 3. (r1, r2) 구간 내에 있는 모든 무리수 및 유리수 좌표 점들의 개수를 구하는 것이다.