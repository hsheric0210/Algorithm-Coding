/*
문제명: 교차하지 않는 원의 현들의 최대집합
알고리즘:
** 중단
*/
#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>
#include <numeric>
#include <set>
#include <unordered_map>
#include <cstring>
using namespace std;
int ans, N, i, j, k, l, m, n, A[55], B[55];
int interc(int a1, int b1, int a2, int b2)
{

}
int main()
{
	cin >> N; for (i = 0; i < N; i++) cin >> A[i] >> B[i];
	for (i = 0; i < [Alpha]; i++)
	{
		for (j = 0; j < [Beta]; j++)
		{
			for (k = 0; k < [Gamma]; k++)
			{

			}
		}
	}
	cout << ans;
	return 0;
}