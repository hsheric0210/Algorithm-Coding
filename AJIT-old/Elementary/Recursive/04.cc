/* 기초 알고리즘(재귀호출) - 4. 부분집합 모두 출력하기 */

#define _CRT_SECURE_NO_WARNINGS
#define _CRT_NONSTDC_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>
#include <set>
#include <algorithm>
#include <set>
#include <string>

std::set<int> possibility;

void processRecursively(std::string str)
{
	if (possibility.find(flags))
	processRecursively(flags | )
}

int main(void)
{
	FILE* in = fopen("INPUT.TXT", "r");

	int n = 0;
	fscanf(in, "%d", &n);
	fclose(in);


	fclose(out);
	return 0;
}
